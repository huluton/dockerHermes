# hermes-controller 的相依套件。
#
# 全部釘到確切版本。這個服務握有建立容器與晉升程式碼到正式環境的權限 ——
# 供應鏈的變動應該來自一次刻意的更新，而不是某次重建映像時的意外。
#
# 升級流程：改版號、重建、跑一次 `make verify`。

fastapi==0.140.0
uvicorn[standard]==0.51.0
docker==7.2.0
pydantic==2.13.4
PyYAML==6.0.3

# docker 套件的遞移相依。明確釘住，避免破壞性的大版本變更透過遞移相依
# 偷偷跑進來。
requests==2.34.2
urllib3==2.7.0
