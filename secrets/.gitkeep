# 這個目錄以 :ro 掛進 hermes-runtime 的 /opt/gcp。
#
# 要用 Vertex AI 的話，把 GCP service account 的 JSON 放進來，命名成
# gcp-sa.json（或改 .env 的 VERTEX_SA_FILE）。
#
# 目錄是空的也沒關係 —— hermes 找不到憑證檔就會跳過 Vertex，不影響用其他
# provider 的人。
#
# 這裡的內容不進版控（見 .gitignore），只有這個 .gitkeep 例外。
